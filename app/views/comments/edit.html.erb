<div class="container">
    <div class="card shadow-sm mb-10 bg-white rounded">
      <h1 class="text-center card-header bg-dark text-white"><%= @post.title %></h1>
<%= render partial: 'form', locals: {post: @post, comment: @comment, path: post_comment_path(@post, @comment)} %>
</div>
  </div>